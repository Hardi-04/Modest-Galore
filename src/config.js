export const WHATSAPP_NUMBER = '233256459524';

// Fallback contact options for devices without WhatsApp.
export const SMS_NUMBER_E164 = '+233256459524';
export const INSTAGRAM_USERNAME = 'shopwth_haajar';

// For WhatsApp image links to work for the seller, this must be a public URL (not localhost).
// Example: https://modestgalore.com
export const PUBLIC_SITE_URL = (import.meta.env?.VITE_PUBLIC_SITE_URL || '').trim();

